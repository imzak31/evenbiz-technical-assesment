<div class="space-y-6">
  <!-- Header -->
  <div class="flex items-center gap-4">
    <%= link_to profile_path, class: "text-gray-400 hover:text-gray-500" do %>
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
    <% end %>
    <h1 class="text-2xl font-bold text-gray-900">Edit Profile</h1>
  </div>

  <div class="max-w-2xl">
    <div class="bg-white shadow rounded-lg overflow-hidden">
      <div class="px-6 py-5 border-b border-gray-200">
        <h3 class="text-lg font-medium text-gray-900">Account Settings</h3>
        <p class="mt-1 text-sm text-gray-500">Update your profile information</p>
      </div>
      
      <div class="p-6">
        <%= form_with model: @user, url: profile_path, method: :patch, class: "space-y-6", data: { controller: "form", action: "submit->form#submit" } do |form| %>
          <%= render "shared/form_errors", resource: @user %>

          <!-- Profile Section -->
          <div class="space-y-4">
            <h4 class="text-sm font-medium text-gray-900 border-b border-gray-200 pb-2">Profile Information</h4>
            
            <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
              <div>
                <%= form.label :first_name, "First Name", class: "block text-sm font-medium text-gray-700" %>
                <%= form.text_field :first_name, 
                    class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
              </div>

              <div>
                <%= form.label :last_name, "Last Name", class: "block text-sm font-medium text-gray-700" %>
                <%= form.text_field :last_name, 
                    class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
              </div>
            </div>

            <div>
              <%= form.label :avatar, "Profile Picture", class: "block text-sm font-medium text-gray-700" %>
              <% if @user.avatar.attached? %>
                <div class="mt-2 flex items-center gap-4">
                  <%= image_tag @user.avatar, class: "h-16 w-16 rounded-full object-cover" %>
                  <span class="text-sm text-gray-500">Current avatar</span>
                </div>
              <% end %>
              <%= form.file_field :avatar, 
                  accept: "image/*",
                  class: "mt-2 block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-indigo-50 file:text-indigo-700 hover:file:bg-indigo-100" %>
            </div>
          </div>

          <!-- Email Section -->
          <div class="space-y-4">
            <h4 class="text-sm font-medium text-gray-900 border-b border-gray-200 pb-2">Email Address</h4>
            
            <div>
              <%= form.label :email, "Email", class: "block text-sm font-medium text-gray-700" %>
              <%= form.email_field :email, 
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
            </div>
          </div>

          <!-- Password Section -->
          <div class="space-y-4">
            <h4 class="text-sm font-medium text-gray-900 border-b border-gray-200 pb-2">Change Password</h4>
            <p class="text-sm text-gray-500">Leave blank if you don't want to change your password.</p>
            
            <div>
              <%= form.label :password, "New Password", class: "block text-sm font-medium text-gray-700" %>
              <%= form.password_field :password, 
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
                  autocomplete: "new-password" %>
            </div>

            <div>
              <%= form.label :password_confirmation, "Confirm New Password", class: "block text-sm font-medium text-gray-700" %>
              <%= form.password_field :password_confirmation, 
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
                  autocomplete: "new-password" %>
            </div>
          </div>

          <!-- Actions -->
          <div class="flex justify-end gap-3 pt-4 border-t border-gray-200">
            <%= link_to "Cancel", profile_path, class: "rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" %>
            <%= form.submit "Save Changes",
                class: "rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 disabled:opacity-50 disabled:cursor-not-allowed",
                data: { form_target: "submit" } %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
